#ifndef dep_hpp
#define dep_hpp

#include <stdio.h>

#endif /* dep_hpp */
using namespace std;
class MyCalulate
{
private:
    int inta,intb;
public:
    void myAdd();
    int myAdd(int a, int b);
    MyCalulate(int a, int b);
    ~MyCalulate();
};
